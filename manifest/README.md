# fnOS manifest placeholder

The final fnOS manifest schema and required fields are not yet confirmed. Do not treat this directory as a valid production manifest.

TODO: verify fnOS package identifier, architecture, permissions, service registration, web integration, signing, and release metadata fields before publishing.
